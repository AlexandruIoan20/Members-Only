doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel='stylesheet', href='/stylesheets/style.css')
        title Document
    body 
        block content 
            header 
                article.main-header
                    h1 Requests
                nav  
                    ul 
                        li 
                            a(href="/general") ⌂
                        li 
                            a(href="/general/info") i

            main 
                if reqUser.grade == "Admin" || reqUser.grade == "Owner"
                    if requests.length 
                        for request in requests 
                            p.request 
                                p #{request.user.username} - #{request.user.grade} 
                                    a(href=`${request.url}`) View 
                                
                    else 
                        p No requests yet. 
