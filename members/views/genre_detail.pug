doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel='stylesheet', href='/stylesheets/style.css')
        title Document
    body 
    block content 
        header 
            article.main-header
                h1 #{genre.title}
            ul 
                li 
                    a(href="/general") ⌂
                li 
                    a(href=user.url) A 
                li 
                    a(href="/general/info") i

        main 
            p.post-description #{genre.description}

        if user.grade == "Admin" || user.grade == "Owner"
            section.developer 
                p 
                    a.button(href= `${genre.url}/delete`) Delete Genre 
                p 
                    a.button(href= `${genre.url}/update`) Update Genre 